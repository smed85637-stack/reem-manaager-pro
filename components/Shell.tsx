"use client";import Link from "next/link";import {usePathname,useRouter} from "next/navigation";import {supabase} from "@/lib/supabase";
const items=[["لوحة التحكم","/dashboard"],["الزبائن","/customers"],["الخدمات","/services"],["الباقات","/packages"],["الطلبات","/orders"],["الديون","/debts"],["التقارير","/reports"],["الإعدادات","/settings"]];
export function Shell({children}:{children:React.ReactNode}){const path=usePathname();const router=useRouter();async function out(){await supabase().auth.signOut();router.push('/login')}return <div><aside className="fixed inset-y-0 right-0 hidden w-72 flex-col bg-gradient-to-b from-navy to-blue-950 p-5 text-white lg:flex"><div className="mb-8 flex items-center gap-3"><div className="grid h-12 w-12 place-items-center rounded-2xl bg-primary text-xl font-black">R</div><div><b>ريم للشحن الإلكتروني</b><p className="text-xs text-blue-200">Reem Manager Pro</p></div></div><nav className="flex flex-1 flex-col gap-2">{items.map(([t,h])=><Link key={h} href={h} className={`rounded-2xl px-4 py-3 text-sm font-bold ${path.startsWith(h)?'bg-primary':'hover:bg-white/10'}`}>{t}</Link>)}</nav><button onClick={out} className="btn bg-white/10 text-white">خروج</button></aside><main className="min-h-screen lg:pr-72">{children}</main></div>}
export function Header({title,sub}:{title:string;sub?:string}){return <header className="mb-6 rounded-3xl border border-slate-200 bg-white p-5 shadow-soft"><h1 className="text-3xl font-black">{title}</h1>{sub&&<p className="mt-1 text-sm text-slate-500">{sub}</p>}</header>}
export function Stat({title,value,tone='text-primary'}:{title:string;value:any;tone?:string}){return <div className="card"><p className="text-sm text-slate-500">{title}</p><p className={`mt-3 text-4xl font-black ${tone}`}>{value}</p></div>}
export function Badge({s}:{s:string}){const c=s==='مدفوع'?'bg-green-100 text-green-700':s==='جزئي'?'bg-orange-100 text-orange-700':'bg-rose-100 text-rose-700';return <span className={`rounded-full px-3 py-1 text-xs font-bold ${c}`}>{s}</span>}
