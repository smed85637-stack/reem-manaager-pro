"use client";import {useState} from "react";import {useRouter} from "next/navigation";import {supabase} from "@/lib/supabase";
export default function Login(){const r=useRouter(),sb=supabase();const[mode,setMode]=useState<'login'|'signup'>('login');const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[msg,setMsg]=useState('');async function submit(e:React.FormEvent){e.preventDefault();setMsg('');const res=mode==='login'?await sb.auth.signInWithPassword({email,password}):await sb.auth.signUp({email,password});if(res.error)return setMsg(res.error.message);r.push('/onboarding');r.refresh()}return <main className="grid min-h-screen place-items-center p-4"><form onSubmit={submit} className="card w-full max-w-md"><div className="mb-6 text-center"><div className="mx-auto mb-3 grid h-16 w-16 place-items-center rounded-3xl bg-primary text-2xl font-black text-white">R</div><h1 className="text-3xl font-black">Reem Manager Pro</h1><p className="text-sm text-slate-500">ريم لإدارة الشحن الإلكتروني</p></div><input className="input mb-3" type="email" placeholder="البريد الإلكتروني" value={email} onChange={e=>setEmail(e.target.value)} required/><input className="input mb-3" type="password" placeholder="كلمة المرور" value={password} onChange={e=>setPassword(e.target.value)} required minLength={6}/>{msg&&<p className="mb-3 rounded-xl bg-red-50 p-3 text-sm text-red-700">{msg}</p>}<button className="btn-primary w-full">{mode==='login'?'تسجيل الدخول':'إنشاء حساب'}</button><button type="button" className="mt-4 w-full text-sm font-bold text-primary" onClick={()=>setMode(mode==='login'?'signup':'login')}>{mode==='login'?'إنشاء حساب جديد':'لديك حساب؟ تسجيل الدخول'}</button></form></main>}
