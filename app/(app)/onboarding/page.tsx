"use client";import {useEffect,useState} from "react";import {useRouter} from "next/navigation";import {supabase} from "@/lib/supabase";
export default function Onboarding(){const sb=supabase(),r=useRouter();const[name,setName]=useState('محلي');const[phone,setPhone]=useState('');useEffect(()=>{(async()=>{const{data:{user}}=await sb.auth.getUser();if(!user)return r.push('/login');const{data}=await sb.from('stores').select('id').eq('owner_id',user.id).maybeSingle();if(data)r.push('/dashboard')})()},[]);async function save(e:React.FormEvent){e.preventDefault();const{data:{user}}=await sb.auth.getUser();if(!user)return;const{error}=await sb.from('stores').insert({owner_id:user.id,store_name:name,phone});if(error)return alert(error.message);r.push('/dashboard')}return <main className="container-page grid min-h-screen place-items-center"><form onSubmit={save} className="card w-full max-w-lg"><h1 className="mb-4 text-3xl font-black">إعداد المحل</h1><input className="input mb-3" value={name} onChange={e=>setName(e.target.value)} placeholder="اسم المحل"/><input className="input mb-5" value={phone} onChange={e=>setPhone(e.target.value)} placeholder="رقم الهاتف"/><button className="btn-primary w-full">حفظ والدخول</button></form></main>}
