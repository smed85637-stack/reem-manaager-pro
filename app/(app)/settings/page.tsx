"use client";import {useEffect,useState} from "react";import {Header} from "@/components/Shell";import {supabase} from "@/lib/supabase";
export default function Settings(){const sb=supabase();const[id,setId]=useState(''),[name,setName]=useState(''),[phone,setPhone]=useState('');useEffect(()=>{(async()=>{const{data:{user}}=await sb.auth.getUser();if(!user)return;const{data}=await sb.from('stores').select('*').eq('owner_id',user.id).maybeSingle();if(data){setId(data.id);setName(data.store_name);setPhone(data.phone||'')}})()},[]);async function save(e:React.FormEvent){e.preventDefault();const{error}=await sb.from('stores').update({store_name:name,phone}).eq('id',id);if(error)return alert(error.message);alert('تم الحفظ')}return <div className="container-page"><Header title="الإعدادات"/><form onSubmit={save} className="card max-w-xl"><input className="input mb-3" value={name} onChange={e=>setName(e.target.value)}/><input className="input mb-5" value={phone} onChange={e=>setPhone(e.target.value)}/><button className="btn-primary">حفظ</button></form></div>}
